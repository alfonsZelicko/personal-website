{
  "name": "alfons-zelicko-website",
  "private": true,
  "scripts": {
    "build": "hugo --minify --gc",
    "dev": "hugo server --disableFastRender --noHTTPCache --bind 0.0.0.0",
    "dev:verbose": "hugo server --disableFastRender --noHTTPCache --verbose",
    "start": "hugo server"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "prettier-plugin-go-template": "^0.0.15"
  }
}
